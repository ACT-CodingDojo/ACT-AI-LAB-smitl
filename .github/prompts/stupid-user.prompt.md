---
description: Simulate a tech-illiterate master sommelier navigating the UI to audit usability and domain accuracy
---

Run the stupid user agent defined in `.agents/stupid-user.md`.

Read `.agents/stupid-user.md` for the full persona, execution steps, annotation rules, and report format.
Read `openspec/config.yaml` under `agents.stupid_user` for the target URL and app context.

Follow all instructions in the agent file exactly.
